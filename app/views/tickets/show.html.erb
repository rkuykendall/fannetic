<div class="row">
  <div class="small-12 columns">
    <h1>1 Ticket for <%= link_to @event.title, team_event_path(@team,@event) %> belonging to <%= link_to @fan.name, fan_path(@fan) %></h1>
  </div>
</div>