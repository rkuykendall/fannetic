<div class="row">
  <div class="small-12 columns">
    <h1>Edit Perk</h1>
    <%= form_for :perk, url: team_event_perk_path(@team,@event,@perk), method: :patch do |f| %>

    	<% if @perk.errors.any? %>
      	<div id="error_explanation">
      		<h2><%= pluralize(@perk.errors.count, "error") %> prohibited this event from being saved:</h2>

      		<ul>
        		<% @perk.errors.full_messages.each do |msg| %>
        			<li><%= msg %></li>
        		<% end %>
      		</ul>
      	</div>
    	<% end %>

      <%= render :partial => "form", :locals => {:f => f} %>

      <div>
        <%= f.submit :class => 'button right' %>
      </div>
    <% end %>
  </div>
</div>